import React, { useEffect, useRef } from 'react'

export default function Display({ setInput }) {

    return (
        <div className='display'>
            <input className='text-input' type="text" />
        </div>
    )
}
